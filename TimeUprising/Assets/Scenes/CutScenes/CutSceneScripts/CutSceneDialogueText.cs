using UnityEngine;
using System.Collections;

public class CutSceneDialogueText : MonoBehaviour {
/*
	public GUIText mDialogueText;
	public int SceneNumber;

	string mDialogueString;
	float mLastLetter = 0f;
	int index = 0;

	const float kLetterInter = 0.3f;
	
	// Use this for initialization
	void Start () {
	//	mDialogueString = GameObject.Find("SceneManager").GetComponent<StoryBook>().GetDialogueText(SceneNumber);
		
	}
	
	// Update is called once per frame
	void Update () {
		if(index < mDialogueString.Length)
			if(Time.time - mLastLetter > kLetterInter){
				mDialogueText.text += mDialogueString[index];
				index++;
				mLastLetter = Time.time;
			}
	}
	*/
}
